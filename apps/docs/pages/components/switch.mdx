import { Tab, Tabs } from 'nextra-theme-docs';
import '@cakepop/react/styles';
import { defaultTheme, Switch, Flex } from '@cakepop/react';

# Switch

#### Variant

<Tabs items={['Preview', 'TSX']}>
  <Tab>
    <div className={defaultTheme}>
      <Flex>
        <Switch defaultChecked variant='neutral' />
        <Switch defaultChecked variant='primary' />
        <Switch defaultChecked variant='accent' />
        <Switch defaultChecked variant='success' />
        <Switch defaultChecked variant='info' />
        <Switch defaultChecked variant='warning' />
        <Switch defaultChecked variant='danger' />
      </Flex>
    </div>

  </Tab>
  <Tab>
    ```tsx //
        import { Switch } from '@cakepop/react';

        <Switch defaultChecked variant='neutral' />
        <Switch defaultChecked variant='primary' />
        <Switch defaultChecked variant='accent' />
        <Switch defaultChecked variant='success' />
        <Switch defaultChecked variant='info' />
        <Switch defaultChecked variant='warning' />
        <Switch defaultChecked variant='danger' />
    ```

  </Tab>
</Tabs>

#### Size

<Tabs items={['Preview', 'TSX']}>
  <Tab>
    <div className={defaultTheme}>
      <Flex>
        <Switch defaultChecked size='xs' />
        <Switch defaultChecked size='sm' />
        <Switch defaultChecked size='md' />
        <Switch defaultChecked size='lg' />
        <Switch defaultChecked size='xl' />
      </Flex>
    </div>

  </Tab>
  <Tab>
    ```tsx //
        import { Switch } from '@cakepop/react';

        <Switch defaultChecked size='xs' />
        <Switch defaultChecked size='sm' />
        <Switch defaultChecked size='md' />
        <Switch defaultChecked size='lg' />
        <Switch defaultChecked size='xl' />
    ```

  </Tab>
</Tabs>

#### Square

<Tabs items={['Preview', 'TSX']}>
  <Tab>
    <div className={defaultTheme}>
      <Flex>
        <Switch square />
      </Flex>
    </div>

  </Tab>
  <Tab>
    ```tsx //
        import { Switch } from '@cakepop/react';

       <Switch>Label</Switch>
    ```

  </Tab>
</Tabs>

#### Label

<Tabs items={['Preview', 'TSX']}>
  <Tab>
    <div className={defaultTheme}>
      <Flex>
        <Switch>Label</Switch>
      </Flex>
    </div>

  </Tab>
  <Tab>
    ```tsx //
        import { Switch } from '@cakepop/react';

       <Switch>Label</Switch>
    ```

  </Tab>
</Tabs>

